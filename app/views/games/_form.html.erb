<%= form_for(@game, :html => {:class => "form-horizontal"}) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<br>

  <div class="input">
    <%= f.label :name, :class => "help-inline" %><br>
    <%= f.text_field :name, :class => "span5" %>
  </div>

<br>

  <div class="input">
    <%= f.label :store_url, :class => "help-inline" %><br>
    <%= f.text_area :store_url, :class => "span5" %>
  </div>

<br>

  <div class="input-prepend">
    <%= f.label :description, :class => "help-inline" %><br>
    <%= f.text_area :description, :class => "span5", :rows => "5" %>
  </div>

<br>

  <div class="input">
    <%= f.label :logo_url, :class => "help-inline" %><br>
    <%= f.text_area :logo_url, :class => "span5" %>
  </div>

<br><br>
  <div class="actions">
    <%= f.submit "Add Game", :class => "btn btn-success" %>
  </div>
<% end %>
